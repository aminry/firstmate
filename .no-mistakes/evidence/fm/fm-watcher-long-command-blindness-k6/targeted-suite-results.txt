Targeted suites driving the changed behavior (all exit 0):

$ bash tests/fm-watch-triage.test.sh    # 117 checks, exit 0
ok - crew_pipeline_activity_is_recent: only a working run step that reports recent activity is evidence
ok - with the pipeline opt-in absent, a quiet pane writing its own worktree is still deferred and one writing nothing still wedge-escalates on the unchanged schedule
ok - a quiet pane whose pipeline reports an active step is deferred, while one with no activity report still wedge-escalates on the unchanged schedule
ok - a pipeline deferral re-surfaces once on the bounded pause cadence, so an unproductive run cannot stay invisible
ok - without config/wedge-defer-pipeline a pipeline-active crew still wedge-escalates on the unchanged schedule and writes no deferral state

$ bash tests/fm-crew-state.test.sh       # exit 0
ok - a working run publishes the pipeline-activity marker only while the pipeline reports it active
ok - the pipeline-activity marker is published only in a home that opted in
ok - a coarse runs-ledger verdict never publishes another crew's pipeline activity

$ bash tests/fm-secondmate-harness.test.sh  # exit 0
ok - B1 propagate_inheritable_config: copy, idempotence, convergence, absence-mirror, exclusion, no-op, skip diagnostics
ok - B4 spawn: no config at all -> own harness and no propagation side effects
ok - B8 bootstrap sweep propagates config even when the home's tracked files are already current
ok - B12c presentation inheritance: the primary default converges on, and only an explicit opt-out propagates off
